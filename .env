# Docker settings
DOCKER_IMAGE_TAG=dev
# we let it be defined by the folder name in dev
# COMPOSE_PROJECT_NAME=explorer
# unify separator with windows style
COMPOSE_PATH_SEPARATOR=;
COMPOSE_FILE=docker-compose.yml;docker/dev.yml
# no auto restart in dev
RESTART_POLICY=no
